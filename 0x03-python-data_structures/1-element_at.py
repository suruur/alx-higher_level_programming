#!/usr/bin/python3
def element_at(my_list, idx):

    if __name__ == "__main__":
        if idx < 0:
            return ("None")
        elif idx > len(my_list):
            return ("None")
        else:
            for i in my_list:
                return (my_list[idx])
